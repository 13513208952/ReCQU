# Contributing to ReCQU

感谢你帮助改进 ReCQU。

## 提交前

1. 先搜索现有 Issue，避免重复报告。
2. 功能应保持非官方登录完全由用户在学校页面手动完成。
3. 不得提交账号、密码、Cookie、Token、验证码、CAS Ticket、真实成绩、真实课表或未脱敏抓包。
4. 不得加入绕过验证码、访问控制、限流、证书校验或校园网检测的实现。
5. 新模块应具有独立的缓存和失败边界，不能用不完整响应覆盖完整缓存。

## 开发与验证

需要 JDK 17 和 Android SDK 36：

```bash
./gradlew testDebugUnitTest lintDebug assembleDebug
```

测试数据必须为明显虚构的数据。涉及学校接口的变更只记录请求方法、路径和脱敏后的字段结构。

## Pull Request

- 保持改动范围单一，并说明用户可见行为；
- 为解析、日期计算和缓存迁移补充单元测试；
- 不提交 APK、签名文件、IDE配置或本地 SDK路径；
- 提交即表示你有权提供相关代码，并同意按本仓库的 `AGPL-3.0-only`许可证发布贡献。
